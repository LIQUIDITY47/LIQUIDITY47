install: Verify Human Identity
on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Verified Agent Skill
        run: |
       name: Install Verified Agent Skill
  run: |
    npx clawhub@latest install verified-agent-identity || echo "Skill not available, continuing..."
    cd scripts
    npm install
          all cd scripts
          npm install

      - name: Create Identity & Generate Reward Link
        run: |
          node scripts/createNewEthereumIdentity.js
          node scripts/manualLinkHumanToAgent.js --challenge '{"name": "jude ogbeide", "description": "jude ogbeide and Jude company in Nigeria"}'
